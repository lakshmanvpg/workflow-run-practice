name: Workflow A

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Start Build
        run: echo "Workflow A - Build started"

      - name: Build Application
        run: echo "Workflow A - Building application"

      - name: Build Complete
        run: echo "Workflow A - Build completed"